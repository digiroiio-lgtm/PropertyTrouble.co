# PostgreSQL connection string. Optional for `npm run build` — the public site
# builds and runs without a database. Required for migrations, seeding and the
# /internal debug views (development only).
DATABASE_URL="postgresql://user:password@localhost:5432/propertytrouble"
NEXT_PUBLIC_SITE_URL="https://propertytrouble.com"
